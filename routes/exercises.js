import { Router } from "express";

const router = Router();

/*
Form data description, 
duration, and optionally date. 
If no date is supplied, the current 
date will be used.
*/

//Devuelve Exercise schema
router.post("/", (req, res) => {});



export default router;
